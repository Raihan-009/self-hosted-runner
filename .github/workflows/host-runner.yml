name: Self-Hosted Runner Test

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Check Kubernetes Resources
        run: |
          echo "From self hoster runner..."